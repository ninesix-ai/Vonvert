<!-- SPDX-License-Identifier: Apache-2.0 -->
<!-- Copyright (c) 2026 ninesix-ai studio -->

<Window x:Class="Vonvert.App.ExportDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Vonvert.App.UIServices"
        xmlns:vc="clr-namespace:Vonvert.App.Controls"
        Title="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=ExportDialogTitle, Mode=OneWay}"
        Width="420" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{DynamicResource Bg}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Format selector -->
        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <vc:SelectableTextBlock Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=ExportFormatLabel, Mode=OneWay}" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <ComboBox x:Name="FormatCombo" SelectedIndex="0" SelectionChanged="FormatChanged">
                <ComboBoxItem Content="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=ExportFormatWav, Mode=OneWay}"/>
                <ComboBoxItem Content="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=ExportFormatMp3, Mode=OneWay}"/>
                <ComboBoxItem Content="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=ExportFormatFlac, Mode=OneWay}"/>
                <ComboBoxItem Content="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=ExportFormatOgg, Mode=OneWay}"/>
                <ComboBoxItem Content="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=ExportFormatAac, Mode=OneWay}"/>
            </ComboBox>
        </StackPanel>

        <!-- Parameter panel (dynamically shown) -->
        <ContentControl x:Name="ParameterPanel" Grid.Row="1" Margin="0,0,0,12"/>

        <!-- File path -->
        <StackPanel Grid.Row="2" Margin="0,0,0,12">
            <vc:SelectableTextBlock Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=OutputFileLabel, Mode=OneWay}" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <TextBox x:Name="FilePathBox" IsReadOnly="True"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Style="{StaticResource OutlineButton}" Content="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=ExportRecording, Mode=OneWay}" Width="80" Margin="0,0,8,0" Click="Export_Click"/>
            <Button Style="{StaticResource OutlineButton}" Content="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Cancel, Mode=OneWay}" Width="80" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
